describe("A test to show the use of 'jasmine' with node", () => {

    it('should show the use of "jasmine"', () => {
        expect(true).toBe(true);
    });

    it('should show the use of "karma" with the listener møde', () => {
        expect(1).toEqual(1);
    });
});